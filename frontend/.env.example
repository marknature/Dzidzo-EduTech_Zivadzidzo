# Points the app at your local or deployed ZivaDzidzo backend.
EXPO_PUBLIC_API_URL=http://127.0.0.1:5000
EXPO_PUBLIC_EAS_PROJECT_ID=your-eas-project-id

# Supabase project (publishable/anon public key only - never the service role key here).
# Required for the authenticated, tenant-scoped product. Without them the app shows
# setup guidance rather than exposing an unauthenticated assessment workflow.
EXPO_PUBLIC_SUPABASE_URL=
# Preferred current Supabase variable name.
EXPO_PUBLIC_SUPABASE_KEY=
# Legacy projects may use this instead; do not set both unless intentionally rotating.
# EXPO_PUBLIC_SUPABASE_ANON_KEY=
