// Picked up by Expo's Metro web CSS pipeline to expand global.css's @tailwind directives
// into real utility-class CSS. Native platforms don't need this - NativeWind's babel
// plugin compiles className to style objects directly for them.
module.exports = {
  plugins: {
    tailwindcss: {},
  },
};
