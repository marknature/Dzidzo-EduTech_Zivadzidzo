# Historical local research material is deliberately excluded from any backend package.
archive/

# Deployment packages must never carry local credentials or installed dependencies.
.env
.env.*
!.env.example
node_modules/
